
import { InspectionData } from "../types";

// Serviço de IA desativado.
// A funcionalidade de análise com IA foi removida da aplicação.
export const analyzeChecklist = async (data: InspectionData): Promise<string> => {
  return "Análise de IA desativada.";
};
